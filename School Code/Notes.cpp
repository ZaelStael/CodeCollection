#include <iostream>
#include <string>
using namespace std;

int main()
{
    cout << "int = whole number values to be used in calculations" << endl;

    cout << "float = a decimal with up to 7 digits of precidsion, used for calculations" << endl;

    cout << "short = an integer witnin the range of plus or minus 32,767" << endl;

    cout << "double = an integer within up to 15 digits of precision, used in calculations" << endl;

    cout << "bool = True or False" << endl;

    cout << "binary is calculated by multiplying the value by the position it is in starting from the far right (i.e. 11010 in binary/base2 = 26 in decimal/base10)" << endl;
